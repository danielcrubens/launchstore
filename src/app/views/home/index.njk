{% extends "layout.njk" %}
{% block content %}
    <section class="shop container">
        <h2>Últimos produtos</h2>
        <div class="items">
            {% for product in products %}
                <div class="product">
                    <div class="image" style="background:url({{ product.img if product.img else '//placehold.ti/500x500' }})">
                    {%if product.oldPrice != product.price%}
                    <div class="old-price">
                        {{product.oldPrice}}
                    </div>
                    {%endif%}
                        <p>{{produc.price}}</p>
                        <h4>{{product.name}}</h4>
                            <a href="/products/{{product.id}}" class="button">Ver Produto</a>
                    </div>
                </div>
            </div>
            {%endfor%}
        </section>
    {% endblock content %}